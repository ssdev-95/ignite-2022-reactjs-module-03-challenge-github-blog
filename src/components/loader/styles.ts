import { createStyles } from '@mantine/core'

export const useStyles = createStyles({
  loader: {
    marginTop: '-90px',
    padding: '2rem',
  },
  header: {
    display: 'flex',
    gap: '1.5rem',
    padding: 0,
  },
  div: {
    flex: 1,
    padding: 0,
  },
})
